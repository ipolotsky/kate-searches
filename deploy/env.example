# Шаблон .env для окружения на VM. Копируется в /srv/kate-searches/{prod,stage}/.env,
# заполняется руками, chmod 600. В git реальные значения НЕ коммитим — только этот пример.
# Один .env общий для web/api/worker/beat (env_file в compose). REDIS_URL/API_BASE_URL/PORT
# задаются в compose.yml, здесь их не дублируем.

# --- окружение ---
STACK=kate-prod                 # kate-prod | kate-stage (= COMPOSE_PROJECT_NAME)
DOMAIN=taskyou.me               # прод: taskyou.me | stage: stage.taskyou.me
WEB_TAG=prod-bootstrap          # перезаписывается deploy.sh на каждый выкат
API_TAG=prod-bootstrap          # перезаписывается deploy.sh на каждый выкат

# --- лимиты ресурсов (stage ниже prod). Есть дефолты в compose, тут — фактические значения ---
WORKER_CONCURRENCY=2            # prod=2, stage=1
WEB_MEM=640m                    # stage=448m
API_MEM=512m                    # stage=384m
WORKER_MEM=768m                 # stage=448m
BEAT_MEM=160m                   # stage=128m
REDIS_MEM=192m                  # stage=128m

# --- Supabase (свой проект на каждое окружение) ---
DATABASE_URL=postgresql://postgres:PASSWORD@db.PROJECT.supabase.co:5432/postgres
SUPABASE_URL=https://PROJECT.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
NEXT_PUBLIC_SUPABASE_URL=https://PROJECT.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# --- LLM-провайдеры ---
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GEMINI_API_KEY=
LLM_MODEL_SCORE=gemini/gemini-2.0-flash-lite
LLM_MODEL_DRAFT=openai/gpt-5-mini

# --- метеринг/шлюз (в MVP-проде можно не поднимать: прямой SDK, метеринг выключен) ---
LITELLM_BASE_URL=
LITELLM_MASTER_KEY=
LANGFUSE_ENABLED=false
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_HOST=

# --- скрапинг ---
FIRECRAWL_API_KEY=
USER_AGENT=KateSearchesBot/1.0 (+https://taskyou.me)

# --- биллинг Stripe (M6.2), только web. Soft-launch: sk_test_/pk неважен (Checkout хостится).
# Live-charge невозможен, пока тут sk_test_. Флип test->live = замена sk_/whsec_/трёх price_.
# Вебхук: https://${DOMAIN}/api/stripe/webhook (отдельный endpoint и signing secret на режим). ---
APP_BASE_URL=https://taskyou.me
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_STARTER=
STRIPE_PRICE_PRO=
STRIPE_PRICE_AGENCY=

# --- email Resend (M6.3). Пусто = email выключен. Слать из Python/Celery (очередь emails).
# Вебхук Resend -> https://${DOMAIN}/api/resend/webhook (web проксирует на Python + Svix-проверка).
# EMAIL_FROM — с верифицированного поддомена (DKIM/SPF/DMARC). ---
RESEND_API_KEY=
RESEND_WEBHOOK_SECRET=
EMAIL_FROM=KateSearches <notifications@taskyou.me>
